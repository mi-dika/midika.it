1. run `pnpm check` to make sure the code is ready to be committed
2. check all the code not commited yet
3. cluster the commits in logical groups
4. generate a commit message for each group
5. commit the group
6. continue until all the code is committed
